# Overview

This chapter covers basic helper functions used throughout the renderer that don't belong exactly to one pipeline stage. Several of these (disk sampling, ONB) are building blocks used directly in other chapters, where those chapters will link back to here rather than rederiving them.