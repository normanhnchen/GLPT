# Russian Roulette

Russian roulette is a strategy that addresses the problem of expensive rays which don't contribute much to the final render. It terminates the ray based on a calculated termination probability to prevent it from costing expensive calculations further. The Russian roulette estimator is defined as

$$
F_{RR} =
\begin{cases}
0 & \xi < q \\
\frac{F}{1-q} & \xi \ge q
\end{cases}
$$

where $F$ represents the un-weighted throughput and $q$ is the termination probability.

We can prove that it is unbiased by calculating its expected value $E$.

$$
E[F_{RR}] = q\cdot(0) + (1-q)\left( \dfrac{F}{1 - q} \right) = F
$$E

Because $E[F_{RR}] = F$, Russian roulette converges to the correct throughput $F$.

```glsl
if (i > 3) {
    // Termination probability
    float q = 1.0 - max(throughput.r, max(throughput.g, throughput.b));
    float Xi = Pcg3d(rng).x;

    if (Xi < q) {
        // Return radiance so far
        break;
    } else {
        throughput = throughput / (1.0 - q);
    }
}
```

where we start terminating rays after atleast the third bounce. The first few bounces carry high energy, so applying Russian Roulette earlier can risk terminating high-energy paths and increasing variance.

## Updated Throughput

Accounting for Russian roulette, the throughput equations from [8.1 Overview](8.1_Overview.md) become

$$
T_k = \prod_{j=1}^{k} \dfrac{f(\text{p}_j, -\omega_{j-1}, \omega_j)|\text{n}_j\cdot\omega_j|}{\text{PDF}_{\Omega_j}(\omega_j)(1-q_j)}, \quad
T_0 = 1
$$

## Sources

M. Pharr, W. Jakob, and G. Humphreys, "Russian Roulette and Splitting," in Physically Based Rendering: From Theory to Implementation, 3rd ed. San Francisco, CA: Morgan Kaufmann, 2018. [Online]. Available: https://pbr-book.org/3ed-2018/Monte_Carlo_Integration/Russian_Roulette_and_Splitting
